// 创立一个单独存储向量数据的表
-- CreateTable
CREATE TABLE "VectorValue" (
    "id" TEXT NOT NULL,
    "vectorId"  TEXT NOT NULL,      
    "value" float[] DEFAULT '{}';   
    "content" TEXT DEFAULT '';   
    CONSTRAINT "VectorData_pkey" PRIMARY KEY ("id")
);

-- AddForeignKey
ALTER TABLE "VectorData" ADD CONSTRAINT "VectorData_vectorId_fkey" FOREIGN KEY ("vectorId") REFERENCES "Vector"("id") ON DELETE CASCADE ON UPDATE CASCADE;
